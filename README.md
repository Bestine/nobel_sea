# Nobel Sea

To run the devserver:

1. **Install the dependecies**

```bash
npm install
```

2. **Run the dev server**

```bash
npm run dev
```

3. **Open the server in browser**

Click `o` + `enter`
